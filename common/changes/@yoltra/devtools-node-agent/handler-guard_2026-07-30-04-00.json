{
  "changes": [
    {
      "packageName": "@yoltra/devtools-node-agent",
      "comment": "A command handler that throws is now caught and reported instead of becoming an unhandled rejection. The message callback is async and nothing awaits it, so a failure inside it surfaced far from its cause or nowhere at all, while the panel sat waiting for a reply that never came.",
      "type": "patch"
    }
  ],
  "packageName": "@yoltra/devtools-node-agent"
}
