{
  "changes": [
    {
      "packageName": "@yoltra/react",
      "comment": "Adds createYoltra(): one call returns the store plus every typed hook, defaulting to that store so no Provider is required. Adds typed path accessors for useAtomicProp (autocompleted, inferred return type). Stable memo key for useAtomicProps.",
      "type": "minor"
    }
  ],
  "packageName": "@yoltra/react"
}
