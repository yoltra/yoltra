{
  "changes": [
    {
      "packageName": "@yoltra/devtools-ui",
      "comment": "applyPatches now implements RFC 6902 add semantics for arrays: an add op inserts before the target index (shifting) or appends for the - end-of-array token, instead of overwriting the element like replace. This keeps array indices correct for a future or hostile array-add patch.",
      "type": "patch"
    }
  ],
  "packageName": "@yoltra/devtools-ui"
}
