{
  "changes": [
    {
      "packageName": "@yoltra/devtools-ui",
      "comment": "The in-memory hub names the negotiated protocol version by the field the interface and the real hub use. It sent `protocolVersion` where both declare `negotiatedVersion` — harmless only while nothing read it, and the moment something did, the embedded panel would have failed where the real hub worked, in the one path with no server to inspect.",
      "type": "patch"
    }
  ],
  "packageName": "@yoltra/devtools-ui"
}
