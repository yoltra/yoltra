{
  "changes": [
    {
      "packageName": "@yoltra/core",
      "comment": "The wildcard pattern matcher (LooseEventBus) now uses an iterative two-pointer algorithm with backtracking instead of recursing and re-joining path suffixes for each ** position, removing per-suffix string allocation. Wildcard semantics are unchanged.",
      "type": "patch"
    }
  ],
  "packageName": "@yoltra/core"
}
