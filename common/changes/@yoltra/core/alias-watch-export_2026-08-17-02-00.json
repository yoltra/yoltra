{
  "changes": [
    {
      "packageName": "@yoltra/core",
      "comment": "Exports the `AliasWatch` type. `freezeState` takes it as a parameter and was already public, but the type itself was never re-exported, so a caller could not name the argument it was expected to pass — and the generated reference could not document the parameter at all.",
      "type": "minor"
    }
  ],
  "packageName": "@yoltra/core"
}
