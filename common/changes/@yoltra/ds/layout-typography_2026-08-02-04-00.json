{
  "changes": [
    {
      "packageName": "@yoltra/ds",
      "comment": "Adds fifteen primitives: `Stack`, `Inline`, `Grid`, `Container`, `Divider`, `Card`, `Heading`, `Text`, `Link`, `InlineCode`, `Kbd`, `Spinner`, `Skeleton`, `EmptyState` and `VisuallyHidden`.\n\nSpacing is passed per instance as a custom property rather than through modifier classes. Fourteen scale steps across five layout components would be seventy rules shipped to every consumer; one declaration reading a variable is a single rule, and still loses to a class the caller writes.\n\nThe accessibility decisions live in the API rather than being left to each call site. Spinner announces through a status role with a visually-hidden label defaulting to 'Loading', because an indicator nobody can perceive is worse than none. Skeleton is hidden from assistive technology, since announcing a row of grey boxes tells a screen-reader user nothing they can act on. Heading takes a visual size independent of its outline level, so a section that should look small stays the heading it structurally is, and EmptyState takes a configurable heading level for the same reason. Divider is an hr horizontally and a separator role vertically, because a thematic break turned on its side is not one. Link applies noopener with a blank target, which is a hole rather than a preference. Both animations stop under prefers-reduced-motion.\n\nThe package also gains TypeDoc, which it never had: eighty-five pages generated with no warnings, every component carrying a worked example. Types referenced by public props but never exported \u2014 button variant and size, callout kind, field size, alignment, the theme context \u2014 are now exported, since a prop whose type cannot be named is a prop nobody can type against.",
      "type": "minor"
    }
  ],
  "packageName": "@yoltra/ds"
}
