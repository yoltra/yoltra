{
  "changes": [
    {
      "packageName": "@yoltra/devtools-protocol",
      "comment": "computePatches and patchesFromChange now escape JSON Pointer segments per RFC 6902 (~ becomes ~0 and / becomes ~1), so a state key containing / or ~ produces a valid pointer. A key containing a literal . still cannot be represented by a dotted path (documented limitation).",
      "type": "patch"
    }
  ],
  "packageName": "@yoltra/devtools-protocol"
}
